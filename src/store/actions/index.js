import { signUp, logIn, profileData, genderData, profileImage, getUser, logOut, getProfilePhoto,submitSelfie  } from './auth'

export {
    signUp,
    logIn,
    profileData,
    genderData,
    profileImage,
    getUser,
    logOut,
    getProfilePhoto,
    submitSelfie
}